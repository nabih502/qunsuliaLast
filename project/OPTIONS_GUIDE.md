# دليل إضافة الخيارات (Options) في Form Builder

## 📚 الفرق بين التسمية والقيمة

عند إضافة خيارات للحقول (مثل Dropdown, Radio, Checkbox)، يجب فهم الفرق بين:

### 👁️ التسمية (Label)
- **ما يراه المستخدم** في الواجهة
- تكون عادة **بالعربية**
- مثال: `تجديد`, `جواز جديد`, `بدل فاقد`

### 💾 القيمة (Value)
- **ما يُحفظ في قاعدة البيانات**
- تكون عادة **بالإنجليزية**
- مثال: `renewal`, `new`, `replacement`

## 🎯 لماذا هذا التصميم؟

### 1. الثبات
القيم الإنجليزية لا تتغير حتى لو غيرنا الترجمة العربية:
```
التسمية: "تجديد" → يمكن تغييرها إلى "تجديد الجواز"
القيمة: "renewal" → تبقى ثابتة
```

### 2. التوافق مع الكود
الكود البرمجي يعتمد على قيم ثابتة:
```javascript
if (formData.passportType === "renewal") {
  // عرض حقول إضافية خاصة بالتجديد
}
```

### 3. دعم متعدد اللغات
نفس القيمة يمكن عرضها بأي لغة:
```json
{
  "value": "renewal",
  "label_ar": "تجديد",
  "label_en": "Renewal"
}
```

## 🛠️ كيفية إضافة خيارات جديدة

### في Form Builder

1. **اختر الحقل** من Canvas
2. **اذهب إلى Properties Panel**
3. **اضغط "+ Add option"**
4. **املأ البيانات:**
   - **التسمية**: ما يظهر للمستخدم (عربي)
     ```
     مثال: تجديد
     ```
   - **القيمة**: ما يُحفظ في DB (إنجليزي)
     ```
     مثال: renewal
     ```

### في Dynamic List Fields

عند إضافة حقل داخل Dynamic List:
1. اختر نوع الحقل (Select, Radio, etc.)
2. اضغط "+ خيار"
3. املأ:
   - **ما يظهر** 👁️: التسمية بالعربية
   - **value** 💾: القيمة بالإنجليزية

## ⚡ أمثلة عملية

### مثال 1: نوع الجواز
```json
{
  "field_name": "passportType",
  "label_ar": "نوع الطلب",
  "options": [
    { "label_ar": "جواز جديد", "value": "new" },
    { "label_ar": "تجديد", "value": "renewal" },
    { "label_ar": "بدل فاقد", "value": "replacement" }
  ]
}
```

**ما يراه المستخدم:**
```
نوع الطلب: [جواز جديد ▼]
```

**ما يُحفظ في DB:**
```json
{ "passportType": "new" }
```

### مثال 2: نوع الخدمة
```json
{
  "field_name": "serviceType",
  "label_ar": "نوع الخدمة",
  "options": [
    { "label_ar": "زواج", "value": "marriage" },
    { "label_ar": "طلاق", "value": "divorce" }
  ]
}
```

## 🔍 استخدام القيم في الشروط

عند إنشاء شروط، استخدم **القيمة** وليس التسمية:

### ✅ صحيح
```json
{
  "show_when": [
    {
      "field": "passportType",
      "operator": "equals",
      "value": "renewal"  ← استخدم القيمة بالإنجليزية
    }
  ]
}
```

### ❌ خطأ
```json
{
  "show_when": [
    {
      "field": "passportType",
      "operator": "equals",
      "value": "تجديد"  ← لا تستخدم التسمية العربية
    }
  ]
}
```

## 🎨 المزايا الجديدة في الواجهة

### 1. عرض واضح للقيم
عند اختيار قيمة من dropdown، ستشاهد:
```
تجديد (renewal)
```

### 2. تأكيد القيمة المحفوظة
بعد اختيار قيمة، يظهر مربع أزرق:
```
القيمة المحفوظة: renewal
```

### 3. تحذيرات للقيم الناقصة
إذا أضفت تسمية بدون قيمة:
```
⚠️ يجب إضافة قيمة (value) بالإنجليزية لهذا الاختيار
```

### 4. أمثلة توضيحية
في أسفل محرر Options، ستجد أمثلة:
```
💡 مثال:
التسمية: "تجديد"     value: "renewal"
التسمية: "جواز جديد"  value: "new"
```

## 📝 نصائح مهمة

### ✅ افعل
- استخدم أسماء قصيرة واضحة للقيم
- استخدم snake_case أو camelCase
- كن متسقاً في التسمية

### ❌ لا تفعل
- لا تستخدم مسافات في القيم
- لا تستخدم أحرف خاصة
- لا تستخدم العربية في القيم

### أمثلة جيدة للقيم
```
new, renewal, replacement
marriage, divorce
male, female
yes, no, maybe
```

### أمثلة سيئة للقيم
```
جديد, تجديد (عربي)
new passport (مسافة)
yes! (أحرف خاصة)
```

## 🔄 تحويل القيم القديمة

إذا كان لديك شروط قديمة بصيغة:
```json
{ "category": "minors_new" }
```

يمكنك تحويلها إلى الصيغة الجديدة بالضغط على:
```
🗑️ حذف الشروط القديمة وإنشاء جديدة
```

## 🎯 الخلاصة

| الجزء | الاستخدام | مثال |
|------|----------|------|
| **التسمية** | ما يراه المستخدم | تجديد |
| **القيمة** | ما يُحفظ في DB | renewal |
| **الشروط** | تقارن بالقيمة | value: "renewal" |
| **العرض** | يعرض التسمية | "تجديد" |

---

**تذكر:** المستخدم يرى التسمية، لكن النظام يعمل بالقيمة!
