# إصلاحات نظام التتبع

## ✅ التغييرات المطبقة

### 1. توحيد النصوص
- ✅ تغيير "تم الاستلام" إلى "تم التقديم" في جميع الملفات
- ✅ توحيد النصوص بين لوحة التحكم وصفحة التتبع

### 2. Timeline ديناميكي ذكي
**قبل:**
- كان يعرض جميع المراحل بما فيها (موعد، شحن) حتى لو لم تمر بها الحالة

**بعد:**
- ✅ يعرض فقط المراحل التي تمر بها الحالة فعلياً
- ✅ يتحقق من `status_history` لمعرفة المراحل الفعلية
- ✅ إذا لم يكن هناك دفع → لا يظهر "في انتظار الدفع"
- ✅ إذا لم يكن هناك موعد → لا يظهر "حجز الموعد"
- ✅ إذا لم يكن هناك شحن → لا يظهر "الشحن"

#### مثال للمراحل حسب الحالة:

**طلب بسيط (submitted):**
```
✓ تم التقديم
○ قيد المراجعة (سيظهر عند الانتقال)
```

**طلب قيد المراجعة (in_review):**
```
✓ تم التقديم
✓ قيد المراجعة
○ تمت الموافقة (سيظهر عند الانتقال)
```

**طلب مع دفع (payment_pending):**
```
✓ تم التقديم
✓ قيد المراجعة
✓ تمت الموافقة
⏳ في انتظار الدفع
```

**طلب بدون دفع - مباشرة للمعالجة:**
```
✓ تم التقديم
✓ قيد المراجعة
✓ تمت الموافقة
⏳ قيد المعالجة
```

**طلب مرفوض:**
```
✓ تم التقديم
✓ قيد المراجعة
✗ مرفوض
```

### 3. إضافة Header و Footer
- ✅ أضفت `<Header />` في بداية صفحة التتبع
- ✅ أضفت `<Footer />` في نهاية صفحة التتبع
- ✅ الآن الصفحة متسقة مع باقي الموقع

---

## 🔧 الملفات المعدلة

### 1. `/src/pages/TransactionTracking.jsx`
**التغييرات:**
```diff
+ import Header from '../components/Header';
+ import Footer from '../components/Footer';

- 'submitted': 'تم الاستلام',
+ 'submitted': 'تم التقديم',

+ return (
+   <>
+     <Header />
+     <div className="min-h-screen bg-gray-50 py-8" dir="rtl">
+       {/* ... */}
+     </div>
+     <Footer />
+   </>
+ );
```

**Timeline الذكي:**
```javascript
const getTimelineSteps = () => {
  const steps = [];

  // دائماً: تم التقديم
  steps.push({ key: 'submitted', label: 'تم التقديم' });

  // يضاف فقط إذا الحالة تخطت submitted
  if (currentStatusIndex >= 1) {
    steps.push({ key: 'in_review', label: 'قيد المراجعة' });
  }

  // يضاف فقط إذا مرت بمرحلة الدفع
  const hasPayment = ['payment_pending', 'payment_completed'].includes(status) ||
                     statusHistory.some(h => h.new_status.includes('payment'));
  if (hasPayment) {
    steps.push({ key: 'payment', label: '...' });
  }

  // يضاف فقط إذا مرت بمرحلة الموعد
  const hasAppointment = ['appointment_required', 'appointment_booked'].includes(status) ||
                        statusHistory.some(h => h.new_status.includes('appointment'));
  if (hasAppointment) {
    steps.push({ key: 'appointment', label: '...' });
  }

  // ... وهكذا
};
```

### 2. `/src/components/AdminApplicationStatusManager.jsx`
```diff
- { value: 'submitted', label: 'تم الاستلام' }
+ { value: 'submitted', label: 'تم التقديم' }
```

---

## 📊 أمثلة سيناريوهات

### سيناريو 1: طلب جواز سفر (بدون دفع/موعد/شحن)
```
1. ✓ تم التقديم
2. ⏳ قيد المراجعة
3. ○ تمت الموافقة
4. ○ قيد المعالجة
5. ○ جاهز للاستلام
6. ○ مكتمل
```

### سيناريو 2: طلب توكيل (يتطلب دفع وموعد)
```
1. ✓ تم التقديم
2. ✓ قيد المراجعة
3. ✓ تمت الموافقة
4. ⏳ في انتظار الدفع
5. ○ تم الدفع
6. ○ يتطلب حجز موعد
7. ○ تم حجز الموعد
8. ○ قيد المعالجة
9. ○ جاهز للاستلام
10. ○ مكتمل
```

### سيناريو 3: طلب مع شحن
```
1. ✓ تم التقديم
2. ✓ قيد المراجعة
3. ✓ تمت الموافقة
4. ✓ قيد المعالجة
5. ✓ جاهز للشحن
6. ⏳ جاري الشحن
7. ○ تم الشحن
8. ○ تم التوصيل
```

---

## 🎯 الفوائد

1. **وضوح أكبر للمستخدم:**
   - يرى فقط المراحل التي سيمر بها طلبه
   - لا يرى مراحل غير متعلقة به

2. **توحيد اللغة:**
   - نفس النصوص في لوحة التحكم وصفحة التتبع
   - تجربة متسقة

3. **تكامل مع الموقع:**
   - Header و Footer موجودين
   - تصفح سهل بين الصفحات

4. **مرونة:**
   - كل خدمة لها مسار خاص
   - Timeline يتكيف تلقائياً

---

## 🔍 كيفية عمل Timeline الذكي

### الآلية:
1. **يبدأ بـ "تم التقديم"** - دائماً موجود
2. **يتحقق من الحالة الحالية** - `currentStatusIndex`
3. **يتحقق من السجل التاريخي** - `statusHistory`
4. **يضيف فقط المراحل المطلوبة:**
   - إذا وجد دفع في السجل → يضيف مرحلة الدفع
   - إذا وجد موعد في السجل → يضيف مرحلة الموعد
   - إذا وجد شحن في السجل → يضيف مراحل الشحن
5. **يحدد المكتمل/القادم** حسب الترتيب

### مثال كود التحقق:
```javascript
// هل مر بمرحلة الدفع؟
const hasPayment =
  ['payment_pending', 'payment_completed'].includes(application?.status) ||
  statusHistory.some(h => ['payment_pending', 'payment_completed'].includes(h.new_status));

// إذا نعم، أضف المرحلة
if (hasPayment) {
  steps.push({
    key: 'payment',
    label: 'الدفع',
    completed: application?.status === 'payment_completed',
    // ...
  });
}
```

---

## ✅ الخلاصة

الآن النظام:
- ✅ يعرض نصوص موحدة في كل مكان
- ✅ Timeline ديناميكي حسب مسار الطلب
- ✅ Header و Footer موجودين
- ✅ تجربة متسقة ومحسنة

**كل طلب له مساره الخاص الواضح!** 🎉
