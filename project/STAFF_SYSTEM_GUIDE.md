# دليل نظام إدارة الموظفين الكامل

## نظرة عامة

تم إنشاء نظام إدارة موظفين متكامل يعتمد بالكامل على قاعدة بيانات Supabase مع نظام صلاحيات وأمان محكم.

---

## 1. قاعدة البيانات

### الجداول الرئيسية

#### `staff` - جدول الموظفين
```sql
- id (uuid)
- user_id (uuid) → ربط مع auth.users
- employee_number (text) - رقم الموظف الفريد
- full_name_ar (text) - الاسم بالعربي
- full_name_en (text) - الاسم بالإنجليزي
- email (text) - البريد الإلكتروني
- phone (text) - رقم الهاتف
- role_id (uuid) → ربط مع roles
- department_id (uuid) → ربط مع departments
- region_id (uuid) → ربط مع regions
- hire_date (date) - تاريخ التعيين
- status (text) - الحالة: active, inactive, on_leave
- avatar_url (text) - صورة الموظف
- is_active (boolean)
- created_at, updated_at (timestamptz)
```

#### `roles` - جدول الأدوار والصلاحيات
```sql
- id (uuid)
- name (text) - اسم الدور (admin, super_admin, staff, etc.)
- name_ar (text) - الاسم بالعربي
- name_en (text) - الاسم بالإنجليزي
- description_ar (text)
- description_en (text)
- permissions (jsonb) - الصلاحيات التفصيلية
- is_active (boolean)
- created_at (timestamptz)
```

**الأدوار الافتراضية:**
1. `super_admin` - مدير النظام (صلاحيات كاملة)
2. `admin` - مدير (صلاحيات إدارية واسعة)
3. `supervisor` - مشرف (إشراف على الموظفين والطلبات)
4. `staff` - موظف (معالجة الطلبات)
5. `viewer` - عارض (عرض البيانات فقط)

#### `departments` - جدول الأقسام
```sql
- id (uuid)
- name_ar (text) - اسم القسم بالعربي
- name_en (text) - اسم القسم بالإنجليزي
- description_ar (text)
- description_en (text)
- is_active (boolean)
- created_at, updated_at (timestamptz)
```

**الأقسام الافتراضية:**
- الجوازات
- التصديقات
- التوكيلات
- الأحوال المدنية
- التأشيرات
- الشؤون الإدارية
- خدمة العملاء

#### `regions` - جدول المناطق
```sql
- id (uuid)
- name_ar (text) - اسم المنطقة بالعربي
- name_en (text) - اسم المنطقة بالإنجليزي
- code (text) - رمز المنطقة
- is_active (boolean)
- created_at (timestamptz)
```

**المناطق الافتراضية:** 16 مدينة رئيسية في السعودية (الرياض، جدة، مكة، المدينة، إلخ)

#### `staff_services` - ربط الموظفين بالخدمات
```sql
- id (uuid)
- staff_id (uuid) → ربط مع staff
- service_id (uuid) → ربط مع services
- can_process (boolean) - يمكنه معالجة الطلبات
- can_approve (boolean) - يمكنه اعتماد الطلبات
- can_view (boolean) - يمكنه عرض الطلبات
- created_at (timestamptz)
```

---

## 2. نظام الأمان (RLS Policies)

### صلاحيات الجداول

#### جدول `staff`
- **Super Admin & Admin:** صلاحيات كاملة (قراءة، إضافة، تعديل، حذف)
- **الموظف:** يمكنه عرض وتحديث بياناته الخاصة فقط
- **Public:** لا يوجد وصول

#### جدول `roles`
- **Super Admin:** صلاحيات كاملة
- **Admin:** عرض فقط
- **Public:** لا يوجد وصول

#### جدول `departments` و `regions`
- **Super Admin & Admin:** صلاحيات كاملة
- **Authenticated:** عرض البيانات النشطة فقط
- **Public:** لا يوجد وصول

#### جدول `staff_services`
- **Super Admin & Admin:** صلاحيات كاملة
- **الموظف:** يمكنه عرض خدماته فقط
- **Public:** لا يوجد وصول

---

## 3. Edge Functions

### `create-staff-user`
هذه الوظيفة تقوم بإنشاء حساب موظف جديد بشكل كامل.

**الوظيفة:**
1. التحقق من صلاحيات المستخدم (admin أو super_admin فقط)
2. إنشاء حساب في `auth.users` مع البريد وكلمة المرور
3. إضافة metadata للمستخدم (role, full_name_ar, employee_number)
4. إنشاء سجل في جدول `staff` مع كل البيانات
5. في حالة الخطأ، يتم حذف حساب المستخدم تلقائياً (rollback)

**المعاملات المطلوبة:**
```json
{
  "email": "user@example.com",
  "password": "password123",
  "employee_number": "EMP123456789",
  "full_name_ar": "أحمد محمد علي",
  "full_name_en": "Ahmed Mohammed Ali",
  "phone": "0512345678",
  "role_id": "uuid",
  "role_name": "staff",
  "department_id": "uuid",
  "region_id": "uuid",
  "hire_date": "2025-01-01"
}
```

**الاستخدام:**
```javascript
const response = await fetch(
  `${SUPABASE_URL}/functions/v1/create-staff-user`,
  {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${session.access_token}`,
      'apikey': SUPABASE_ANON_KEY
    },
    body: JSON.stringify(data)
  }
);
```

---

## 4. صفحات النظام

### `/admin/staff` - قائمة الموظفين

**المميزات:**
- عرض جميع الموظفين في جدول منظم
- إحصائيات سريعة (إجمالي الموظفين، النشطين، الأقسام، المناطق)
- فلاتر متقدمة:
  - بحث بالاسم، البريد، أو رقم الموظف
  - تصفية حسب القسم
  - تصفية حسب المنطقة
  - تصفية حسب الحالة
- إجراءات سريعة:
  - عرض تفاصيل الموظف
  - تعديل بيانات الموظف
  - تفعيل/تعطيل الموظف
  - حذف الموظف (مع تأكيد)

**Badges ملونة:**
- 🟢 نشط (active)
- 🔴 غير نشط (inactive)
- 🟡 في إجازة (on_leave)

### `/admin/staff/new` - إضافة موظف جديد

**النموذج يحتوي على:**

#### 1. البيانات الأساسية
- رقم الموظف (يتم توليده تلقائياً)
- الاسم الكامل بالعربي (مطلوب)
- الاسم الكامل بالإنجليزي (اختياري)
- رقم الهاتف (مطلوب، يجب أن يبدأ بـ 05)

#### 2. بيانات تسجيل الدخول
- البريد الإلكتروني (مطلوب)
- كلمة المرور (مطلوبة، 6 أحرف على الأقل)
- زر إظهار/إخفاء كلمة المرور

#### 3. معلومات العمل
- الدور الوظيفي (مطلوب)
- القسم (مطلوب)
- المنطقة (مطلوبة)
- تاريخ التعيين (اختياري، افتراضي: اليوم)
- الحالة (نشط أو غير نشط)

#### 4. الخدمات المخصصة
- قائمة بجميع الخدمات المتاحة
- اختيار متعدد (checkboxes)
- يمكن البحث والتمرير
- اختياري (يمكن تركه فارغاً)

**التحقق من البيانات:**
- التحقق من صحة البريد الإلكتروني
- التحقق من طول كلمة المرور
- التحقق من تنسيق رقم الهاتف السعودي
- التحقق من الحقول المطلوبة
- عرض رسائل خطأ واضحة

**عند الحفظ:**
1. استدعاء Edge Function لإنشاء الحساب والموظف
2. ربط الموظف بالخدمات المحددة في `staff_services`
3. عرض رسالة نجاح
4. الانتقال إلى صفحة قائمة الموظفين

---

## 5. كيفية الاستخدام

### إضافة موظف جديد

1. **تسجيل الدخول** كـ Admin أو Super Admin
2. **الذهاب إلى** قائمة الموظفين (`/admin/staff`)
3. **الضغط على** "إضافة موظف جديد"
4. **ملء البيانات:**
   - رقم الموظف (تلقائي أو تعديله)
   - الاسم بالعربي والإنجليزي
   - البريد الإلكتروني
   - كلمة المرور
   - رقم الهاتف
   - اختيار الدور
   - اختيار القسم
   - اختيار المنطقة
   - تحديد الخدمات (اختياري)
5. **الضغط على** "حفظ الموظف"
6. **النجاح!** سيتم إنشاء:
   - حساب في auth.users
   - سجل في جدول staff
   - روابط في staff_services (إذا تم اختيار خدمات)

### البحث والتصفية

1. استخدم **حقل البحث** للبحث بالاسم أو البريد أو رقم الموظف
2. استخدم **القوائم المنسدلة** لتصفية حسب:
   - القسم
   - المنطقة
   - الحالة
3. النتائج تتحدث تلقائياً عند كل تغيير

### تعديل موظف

1. اضغط على أيقونة **التعديل** (قلم أخضر)
2. سيتم فتح نموذج مشابه لنموذج الإضافة
3. عدّل البيانات المطلوبة
4. احفظ التغييرات

### تفعيل/تعطيل موظف

- اضغط على أيقونة **التفعيل/التعطيل**
- سيتم تغيير حالة الموظف مباشرة
- الموظفون المعطلون لا يمكنهم تسجيل الدخول

### حذف موظف

1. اضغط على أيقونة **الحذف** (سلة مهملات حمراء)
2. ستظهر نافذة تأكيد
3. اضغط "حذف" للتأكيد
4. **تحذير:** لا يمكن التراجع عن هذا الإجراء

---

## 6. الأمان والصلاحيات

### صلاحيات الإضافة والتعديل
- فقط **Super Admin** و **Admin** يمكنهم إضافة/تعديل/حذف موظفين
- الموظفون العاديون يمكنهم فقط عرض وتحديث بياناتهم الشخصية

### كلمات المرور
- يتم تخزين كلمات المرور بشكل آمن في Supabase Auth
- لا يتم عرض كلمات المرور في أي مكان
- عند التعديل، إذا لم يتم تغيير كلمة المرور، تبقى القديمة

### البريد الإلكتروني
- يجب أن يكون فريداً لكل موظف
- يُستخدم لتسجيل الدخول

### رقم الموظف
- يجب أن يكون فريداً
- يتم توليده تلقائياً بتنسيق: `EMP + timestamp + random`
- يمكن تعديله قبل الحفظ

---

## 7. API Endpoints

### قراءة البيانات
```javascript
// قراءة جميع الموظفين مع العلاقات
const { data, error } = await supabase
  .from('staff')
  .select(`
    *,
    role:roles(id, name, name_ar, name_en),
    department:departments(id, name_ar, name_en),
    region:regions(id, name_ar, name_en, code)
  `)
  .order('created_at', { ascending: false });

// قراءة الأقسام
const { data: departments } = await supabase
  .from('departments')
  .select('*')
  .eq('is_active', true)
  .order('name_ar');

// قراءة المناطق
const { data: regions } = await supabase
  .from('regions')
  .select('*')
  .eq('is_active', true)
  .order('name_ar');

// قراءة الأدوار
const { data: roles } = await supabase
  .from('roles')
  .select('*')
  .eq('is_active', true)
  .order('name_ar');

// قراءة خدمات موظف محدد
const { data: services } = await supabase
  .from('staff_services')
  .select(`
    *,
    service:services(*)
  `)
  .eq('staff_id', staffId);
```

### تحديث البيانات
```javascript
// تحديث حالة موظف
const { error } = await supabase
  .from('staff')
  .update({ status: 'inactive' })
  .eq('id', staffId);

// ربط موظف بخدمة
const { error } = await supabase
  .from('staff_services')
  .insert({
    staff_id: staffId,
    service_id: serviceId,
    can_process: true,
    can_approve: false,
    can_view: true
  });
```

### حذف البيانات
```javascript
// حذف موظف (يحذف الروابط تلقائياً بسبب ON DELETE CASCADE)
const { error } = await supabase
  .from('staff')
  .delete()
  .eq('id', staffId);
```

---

## 8. الصفحات القادمة (للتطوير)

### `/admin/staff/:id` - تفاصيل الموظف
- عرض كل معلومات الموظف
- الخدمات المرتبطة
- إحصائيات الأداء
- سجل النشاطات

### `/admin/staff/:id/edit` - تعديل الموظف
- نموذج مشابه لنموذج الإضافة
- تعبئة البيانات الحالية
- إمكانية تغيير كلمة المرور (اختياري)

### إدارة الصلاحيات المتقدمة
- تخصيص صلاحيات دقيقة لكل موظف
- تحديد ما يمكنه عرضه/معالجته/اعتماده
- تقييد الوصول حسب المنطقة أو القسم

---

## 9. نصائح وأفضل الممارسات

### إدارة الموظفين
- استخدم أرقام موظفين فريدة ومنظمة
- حافظ على تحديث البيانات باستمرار
- راجع الصلاحيات بشكل دوري

### الأمان
- لا تشارك كلمات المرور
- غيّر كلمات المرور للموظفين المغادرين فوراً
- راجع سجلات النشاط بانتظام

### الأداء
- استخدم الفلاتر لتقليل عدد النتائج المعروضة
- لا تحذف الموظفين، بل عطّلهم (للحفاظ على السجلات)

---

## 10. استكشاف الأخطاء

### "فشل في إنشاء الموظف"
- تأكد من أن البريد الإلكتروني غير مستخدم
- تأكد من أن رقم الموظف فريد
- تحقق من صلاحياتك (admin أو super_admin)

### "غير مصرح"
- تأكد من تسجيل الدخول
- تحقق من دورك (admin أو super_admin مطلوب)

### "خطأ في تحميل البيانات"
- تحقق من اتصالك بالإنترنت
- تأكد من صحة إعدادات Supabase
- راجع console logs للمزيد من التفاصيل

---

## ملخص

تم إنشاء نظام إدارة موظفين متكامل يشمل:
✅ قاعدة بيانات محكمة مع 5 جداول رئيسية
✅ نظام صلاحيات وأمان (RLS) شامل
✅ Edge Function لإنشاء الحسابات
✅ صفحة قائمة الموظفين مع فلاتر متقدمة
✅ صفحة إضافة موظف جديد متكاملة
✅ ربط الموظفين بالخدمات والأقسام والمناطق
✅ 16 منطقة و7 أقسام و5 أدوار افتراضية
✅ واجهات مستخدم جميلة ومتجاوبة

**النظام جاهز للاستخدام الفوري!**
