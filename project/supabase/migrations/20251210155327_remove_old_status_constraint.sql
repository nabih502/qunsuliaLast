/*
  # إزالة قيد الحالة القديم

  1. التغييرات
    - حذف check constraint القديم `valid_status` من جدول `applications`
    - هذا القيد يمنع استخدام الحالات الجديدة من جدول `application_statuses`
    - الآن يمكن استخدام أي حالة موجودة في نظام الحالات الموحد

  2. السبب
    - القيد القديم يسمح فقط بـ 6 حالات محدودة
    - النظام الجديد يدعم أكثر من 15 حالة
    - يجب إزالة القيد لتمكين استخدام جميع الحالات

  3. الأمان
    - التحقق من صحة الحالات يتم الآن من خلال التطبيق
    - جدول `application_statuses` يحتوي على جميع الحالات الصحيحة
*/

-- حذف check constraint القديم
ALTER TABLE applications DROP CONSTRAINT IF EXISTS valid_status;
