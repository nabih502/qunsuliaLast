# دليل دمج قاعدة البيانات مع الخدمات

## التغييرات التي تمت

تم تحويل النظام ليجلب جميع بيانات الخدمات من قاعدة البيانات Supabase بدلاً من الملفات المحلية، مع الاحتفاظ الكامل بالتصميم الحالي.

---

## 1. إضافة Hook لجلب قائمة الخدمات

### الملف: `src/hooks/useServiceData.js`

تمت إضافة دالة `useServices()` التي:
- تجلب جميع الخدمات النشطة من جدول `services`
- تجلب الأنواع الفرعية من جدول `service_types`
- تجلب المتطلبات من جدول `service_requirements`
- تحول البيانات إلى الصيغة المطلوبة للواجهة

```javascript
export function useServices() {
  const [services, setServices] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  // ... جلب البيانات من قاعدة البيانات

  return { services, loading, error };
}
```

---

## 2. تحديث ServicesGrid

### الملف: `src/components/ServicesGrid.jsx`

**التحديثات:**

1. **استبدال مصدر البيانات:**
   - قبل: `servicesConfig` من الملفات المحلية
   - بعد: `useServices()` من قاعدة البيانات

2. **إضافة حالات التحميل والأخطاء:**
   - حالة تحميل مع مؤشر دائري
   - رسالة خطأ مع زر "إعادة المحاولة"
   - عرض عدد الخدمات المحملة

3. **التصميم:**
   - تم الاحتفاظ بنفس التصميم تماماً
   - جميع الألوان والأنماط كما هي
   - نفس تأثيرات الحركة (animations)

---

## 3. تحديث DynamicServiceForm

### الملف: `src/components/DynamicServiceForm.jsx`

**التحديثات الكاملة:**

### أ) الخطوات الثلاث:

1. **بيانات المتقدم** (الخطوة 1):
   - الاسم الرباعي
   - رقم الجواز
   - رقم الجوال
   - البريد الإلكتروني
   - تاريخ الميلاد
   - المهنة
   - المنطقة والمدينة والحي
   - العنوان
   - مكان العمل

2. **تفاصيل الخدمة** (الخطوة 2):
   - يتم جلب الحقول من قاعدة البيانات
   - عرض الحقول حسب `step_id`
   - دعم الشروط المنطقية (Conditional Logic)

3. **المراجعة والدفع** (الخطوة 3):
   - مراجعة جميع البيانات
   - الشروط والأحكام
   - التحقق من OTP قبل الإرسال

### ب) الشريط الجانبي (Sidebar):

يعرض:
- اسم ووصف الخدمة
- الرسوم (مع أيقونة)
- مدة الإنجاز
- المتطلبات (مع الشروط المنطقية)
- المستندات المطلوبة (مع الشروط المنطقية)

### ج) المزايا:

- التحقق من صحة البيانات في كل خطوة
- حفظ المسودة
- طباعة النموذج
- OTP verification قبل الإرسال
- رسائل خطأ واضحة
- تصميم responsive كامل

---

## 4. بنية قاعدة البيانات

### الجداول المستخدمة:

1. **services**: الخدمات الأساسية
   - `id`, `slug`, `name_ar`, `description_ar`
   - `icon`, `category`, `fees`, `duration`
   - `is_active`, `order_index`

2. **service_types**: الأنواع الفرعية
   - `id`, `service_id`, `name_ar`, `description_ar`
   - `config` (JSON للأيقونات والألوان)

3. **service_fields**: حقول النموذج
   - `id`, `service_id`, `step_id`
   - `field_name`, `field_type`, `label_ar`
   - `is_required`, `options`, `conditions`

4. **service_requirements**: المتطلبات
   - `id`, `service_id`, `requirement_ar`
   - `conditions` (JSON للشروط المنطقية)

5. **service_documents**: المستندات
   - `id`, `service_id`, `document_name_ar`
   - `is_required`, `max_size_mb`, `accepted_formats`
   - `conditions` (JSON للشروط المنطقية)

---

## 5. كيفية إضافة خدمة جديدة

### الخطوات:

1. **إضافة الخدمة في جدول services:**
```sql
INSERT INTO services (slug, name_ar, description_ar, icon, category, fees, duration)
VALUES (
  'new-service',
  'خدمة جديدة',
  'وصف الخدمة',
  'FileText',
  'documents',
  '{"base": 100, "currency": "ر.س"}',
  '3-5 أيام'
);
```

2. **إضافة الحقول في جدول service_fields:**
```sql
INSERT INTO service_fields (
  service_id, step_id, step_title_ar,
  field_name, field_type, label_ar,
  is_required
)
VALUES (
  1, -- معرف الخدمة
  'details',
  'تفاصيل الخدمة',
  'document_type',
  'select',
  'نوع المستند',
  true
);
```

3. **إضافة المتطلبات:**
```sql
INSERT INTO service_requirements (service_id, requirement_ar)
VALUES (1, 'صورة من جواز السفر');
```

4. **إضافة المستندات:**
```sql
INSERT INTO service_documents (
  service_id, document_name_ar, is_required,
  max_size_mb, accepted_formats
)
VALUES (
  1,
  'صورة جواز السفر',
  true,
  5,
  '["pdf", "jpg", "png"]'
);
```

---

## 6. الشروط المنطقية (Conditional Logic)

### مثال على حقل شرطي:

```json
{
  "show_when": [
    {
      "field": "passport_type",
      "operator": "equals",
      "value": "new"
    }
  ],
  "logic": "OR"
}
```

### المعاملات المدعومة:

- `equals`: يساوي
- `not_equals`: لا يساوي
- `contains`: يحتوي على
- `not_contains`: لا يحتوي على
- `in`: في القائمة
- `not_in`: ليس في القائمة
- `greater_than` أو `>`: أكبر من
- `less_than` أو `<`: أصغر من
- `is_empty`: فارغ
- `is_not_empty`: غير فارغ

---

## 7. التصميم

### الألوان المستخدمة:

- اللون الأساسي: `#276073`
- اللون الثانوي: `#1e4a5a`
- الأخضر: `#10B981`
- الأحمر: `#EF4444`

### المكونات:

- **ProgressSteps**: مؤشر الخطوات في الأعلى
- **ConditionalRequirements**: المتطلبات في الشريط الجانبي
- **ConditionalDocuments**: المستندات في الشريط الجانبي
- **OTPVerification**: نافذة التحقق من OTP

---

## 8. ملاحظات مهمة

1. **جميع البيانات من قاعدة البيانات**: لا يوجد أي بيانات ثابتة في الكود
2. **التصميم محفوظ تماماً**: لم يتغير أي شيء في التصميم
3. **الأقسام الثابتة**: بيانات المتقدم والمراجعة والدفع لا تتغير
4. **Responsive**: التصميم يعمل على جميع الأحجام
5. **الأداء**: تحميل سريع مع تخزين مؤقت

---

## 9. الخطوات التالية

1. إضافة خدمات إلى قاعدة البيانات
2. اختبار النظام مع خدمات حقيقية
3. إضافة المزيد من أنواع الحقول إذا لزم الأمر
4. تحسين الأداء بإضافة pagination للخدمات

---

## 10. الدعم

في حال وجود أي مشكلة:
1. تحقق من console في المتصفح
2. تحقق من اتصال قاعدة البيانات
3. تأكد من أن جميع الجداول موجودة ومملوءة بالبيانات
4. راجع ملف `useServiceData.js` للتأكد من الاستعلامات

---

تم بحمد الله! 🎉
