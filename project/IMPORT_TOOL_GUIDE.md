# دليل أداة الاستيراد - Import Tool Guide

## 🎯 نظرة عامة

أداة الاستيراد تسمح لك باستنساخ المتطلبات والمرفقات والحقول من خدمة موجودة أو استيرادها من JSON مباشرة.

---

## 📥 طرق الاستيراد

### 1️⃣ **الاستنساخ من خدمة موجودة** (Clone from Service)

أسهل طريقة لنسخ جميع البيانات من خدمة موجودة بالفعل في النظام.

#### كيفية الاستخدام:
1. افتح الخدمة المراد تعديلها
2. انتقل لأي تبويب (متطلبات، مستندات، حقول)
3. انقر على زر **"استيراد"** 📥
4. اختر تبويب **"استنساخ من خدمة"**
5. اختر الخدمة المصدر من القائمة المنسدلة
6. انقر **"استنساخ الآن"**

#### ماذا يتم نسخه؟
- ✅ جميع المتطلبات مع شروطها
- ✅ جميع المستندات مع شروطها
- ✅ جميع الحقول مع إعداداتها
- ✅ الترتيب الأصلي
- ✅ جميع الشروط المنطقية

#### مثال:
```
لديك خدمة "جوازات السفر" كاملة
تريد إنشاء خدمة "تصديق شهادات"
استنسخ من "جوازات السفر" ثم عدل حسب الحاجة
```

---

### 2️⃣ **الاستيراد من JSON**

طريقة مرنة لاستيراد البيانات من ملف JSON أو من كود JavaScript قديم.

#### كيفية الاستخدام:
1. افتح الخدمة المراد تعديلها
2. انقر على زر **"استيراد"** 📥
3. اختر تبويب **"استيراد JSON"**
4. الصق JSON في المربع النصي
5. انقر **"استيراد الآن"**

#### هيكل JSON:

```json
{
  "requirements": [
    {
      "requirement_ar": "النص بالعربي",
      "requirement_en": "Text in English",
      "conditions": {
        "show_when": [
          {
            "field": "service_type",
            "operator": "equals",
            "value": "renewal"
          }
        ],
        "logic": "AND"
      }
    }
  ],
  "documents": [
    {
      "document_name_ar": "اسم المستند",
      "document_name_en": "Document Name",
      "description_ar": "الوصف",
      "is_required": true,
      "max_size_mb": 5,
      "allowed_types": ["pdf", "jpg", "png"],
      "conditions": {}
    }
  ],
  "fields": [
    {
      "name": "field_name",
      "label_ar": "التسمية",
      "label_en": "Label",
      "type": "text",
      "is_required": true,
      "placeholder_ar": "مثال",
      "config": {},
      "validation": {},
      "conditions": {},
      "step_number": 1
    }
  ]
}
```

---

## 📋 الحقول المدعومة في JSON

### للمتطلبات (Requirements):

| الحقل | إجباري | النوع | الوصف |
|------|-------|------|-------|
| `requirement_ar` | ✅ | نص | نص المتطلب بالعربي |
| `requirement_en` | ✅ | نص | نص المتطلب بالإنجليزي |
| `conditions` | ❌ | كائن | شروط الظهور |
| `order_index` | ❌ | رقم | ترتيب العرض |

### للمستندات (Documents):

| الحقل | إجباري | النوع | الوصف |
|------|-------|------|-------|
| `document_name_ar` | ✅ | نص | اسم المستند بالعربي |
| `document_name_en` | ✅ | نص | اسم المستند بالإنجليزي |
| `description_ar` | ❌ | نص | وصف المستند بالعربي |
| `description_en` | ❌ | نص | وصف المستند بالإنجليزي |
| `is_required` | ❌ | منطقي | هل المستند إجباري؟ (افتراضي: true) |
| `max_size_mb` | ❌ | رقم | الحجم الأقصى (افتراضي: 5) |
| `allowed_types` | ❌ | مصفوفة | أنواع الملفات المسموحة |
| `conditions` | ❌ | كائن | شروط الظهور |
| `order_index` | ❌ | رقم | ترتيب العرض |

### للحقول (Fields):

| الحقل | إجباري | النوع | الوصف |
|------|-------|------|-------|
| `name` | ✅ | نص | اسم الحقل (بالإنجليزية) |
| `label_ar` | ✅ | نص | التسمية بالعربي |
| `label_en` | ✅ | نص | التسمية بالإنجليزي |
| `type` | ✅ | نص | نوع الحقل (text, select, etc.) |
| `is_required` | ❌ | منطقي | هل الحقل إجباري؟ |
| `placeholder_ar` | ❌ | نص | النص التوضيحي بالعربي |
| `placeholder_en` | ❌ | نص | النص التوضيحي بالإنجليزي |
| `config` | ❌ | كائن | إعدادات إضافية (خيارات، إلخ) |
| `validation` | ❌ | كائن | قواعد التحقق |
| `conditions` | ❌ | كائن | شروط الظهور |
| `step_number` | ❌ | رقم | رقم الخطوة |
| `order_index` | ❌ | رقم | ترتيب العرض |

---

## 🎨 أمثلة عملية

### مثال 1: استيراد متطلبات بسيطة

```json
{
  "requirements": [
    {
      "requirement_ar": "صورة من الهوية الوطنية",
      "requirement_en": "Copy of National ID"
    },
    {
      "requirement_ar": "صورة شخصية حديثة",
      "requirement_en": "Recent personal photo"
    }
  ]
}
```

### مثال 2: استيراد مستندات مع شروط

```json
{
  "documents": [
    {
      "document_name_ar": "صورة الجواز القديم",
      "document_name_en": "Old Passport Copy",
      "description_ar": "صورة واضحة من صفحة البيانات",
      "is_required": true,
      "max_size_mb": 5,
      "allowed_types": ["pdf", "jpg", "png"],
      "conditions": {
        "show_when": [
          {
            "field": "service_type",
            "operator": "equals",
            "value": "renewal"
          }
        ],
        "logic": "AND"
      }
    }
  ]
}
```

### مثال 3: استيراد حقول كاملة

```json
{
  "fields": [
    {
      "name": "service_type",
      "label_ar": "نوع الخدمة",
      "label_en": "Service Type",
      "type": "select",
      "is_required": true,
      "config": {
        "options": [
          {
            "value": "new",
            "label_ar": "جديد",
            "label_en": "New"
          },
          {
            "value": "renewal",
            "label_ar": "تجديد",
            "label_en": "Renewal"
          }
        ]
      },
      "step_number": 1
    },
    {
      "name": "full_name",
      "label_ar": "الاسم الكامل",
      "label_en": "Full Name",
      "type": "text",
      "is_required": true,
      "placeholder_ar": "أدخل الاسم الكامل",
      "step_number": 1
    }
  ]
}
```

### مثال 4: استيراد كامل (متطلبات + مستندات + حقول)

```json
{
  "requirements": [
    {
      "requirement_ar": "صورة من الهوية",
      "requirement_en": "ID Copy"
    }
  ],
  "documents": [
    {
      "document_name_ar": "الهوية الوطنية",
      "document_name_en": "National ID",
      "is_required": true,
      "max_size_mb": 5
    }
  ],
  "fields": [
    {
      "name": "national_id",
      "label_ar": "رقم الهوية",
      "label_en": "ID Number",
      "type": "text",
      "is_required": true
    }
  ]
}
```

---

## 🔄 تحويل من الكود القديم إلى JSON

إذا كانت لديك خدمات في ملفات JavaScript قديمة، استخدم السكريبت المساعد:

### الخطوات:

1. افتح ملف `/scripts/extract-passport-service-data.js`
2. عدّل البيانات حسب خدمتك
3. شغل السكريبت:

```bash
node scripts/extract-passport-service-data.js
```

4. انسخ الـ JSON الناتج
5. استخدم "استيراد JSON" في الواجهة

---

## 📝 نصائح مهمة

### ✅ افعل:
- **راجع البيانات قبل الاستيراد** - تأكد من صحة JSON
- **استخدم الاستنساخ للسرعة** - إذا كانت هناك خدمة مشابهة
- **عدّل بعد الاستيراد** - يمكنك تعديل أي شيء بعد الاستيراد
- **احفظ الخدمة** - لا تنسَ حفظ التغييرات بعد الاستيراد

### ❌ لا تفعل:
- **لا تنسخ من نفس الخدمة** - الأداة تمنع ذلك تلقائياً
- **لا تستورد JSON خاطئ** - ستحصل على رسالة خطأ
- **لا تستورد بيانات ضخمة مرة واحدة** - قسّمها على دفعات

---

## 🛠️ حل المشاكل الشائعة

### المشكلة 1: "خطأ في تحليل JSON"

**السبب:** JSON غير صحيح أو يحتوي على أخطاء syntax

**الحل:**
- تحقق من صحة JSON باستخدام أداة مثل [jsonlint.com](https://jsonlint.com)
- تأكد من إغلاق جميع الأقواس والفواصل
- راجع مثال JSON المرفق في الأداة

### المشكلة 2: "لم يتم استيراد بعض الحقول"

**السبب:** أسماء الحقول غير صحيحة أو ناقصة

**الحل:**
- تأكد من كتابة أسماء الحقول بشكل صحيح
- راجع جدول الحقول المدعومة أعلاه
- الحقول الإجبارية (✅) يجب أن تكون موجودة

### المشكلة 3: "لا أرى البيانات المستوردة"

**السبب:** لم يتم حفظ الخدمة بعد

**الحل:**
- البيانات المستوردة تكون في الذاكرة المؤقتة
- يجب النقر على "حفظ الخدمة" لحفظ التغييرات في قاعدة البيانات

---

## 🎓 أمثلة لخدمات جاهزة

### خدمة جوازات السفر

ملف JSON كامل متاح في:
```
/scripts/extract-passport-service-data.js
```

شغل السكريبت واحصل على JSON جاهز للاستيراد!

---

## 🔗 روابط مفيدة

- **دليل المنطق الشرطي**: `CONDITIONAL_LOGIC_GUIDE.md`
- **دليل بناء النماذج**: `FORM_BUILDER_GUIDE.md`
- **دليل إدارة الخدمات**: `DYNAMIC_SERVICE_MANAGEMENT.md`

---

## 💡 أفكار إضافية

### 1. إنشاء مكتبة قوالب
احفظ JSON لخدماتك الشائعة واستخدمها كقوالب:

```
templates/
  ├── passport-service.json
  ├── attestation-service.json
  └── poa-service.json
```

### 2. مشاركة القوالب
شارك قوالب JSON مع فريقك لتوحيد الخدمات

### 3. النسخ الاحتياطي
استخدم الاستنساخ لعمل نسخة احتياطية قبل التعديلات الكبيرة

---

## ✨ الخلاصة

أداة الاستيراد توفر عليك ساعات من العمل اليدوي:

- ⚡ استنساخ سريع من خدمات موجودة
- 📝 استيراد من JSON بمرونة كاملة
- 🔄 تحويل سهل من الكود القديم
- ✅ الحفاظ على جميع الشروط والإعدادات

**وقت التنفيذ:** من ساعات إلى دقائق! 🎉
