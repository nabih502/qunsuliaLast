# نظام التتبع الشامل - دليل كامل

## 🎯 نظرة عامة

تم بناء نظام تتبع متكامل يغطي **جميع** حالات الطلبات من البداية إلى النهاية مع برمجة تفاعلية كاملة لكل حالة.

---

## 📊 الجداول في قاعدة البيانات

### 1. `applications` (الجدول الرئيسي)
- معلومات الطلب الأساسية
- الحالة الحالية (`status`)
- بيانات النموذج (`form_data`)

### 2. `payments` (جدول المدفوعات)
- تسجيل جميع عمليات الدفع
- رقم العملية (`transaction_id`)
- المبلغ وطريقة الدفع
- حالة الدفع

### 3. `status_history` (سجل التغييرات)
- تسجيل تلقائي لكل تغيير في الحالة
- الحالة القديمة والجديدة
- من قام بالتغيير
- ملاحظات التغيير

### 4. `appointments` (المواعيد)
- حجوزات المواعيد
- التاريخ والوقت
- المنطقة
- الحالة (confirmed, cancelled, completed)

### 5. `appointment_slots` (الأوقات المتاحة)
- المواعيد المتاحة للحجز
- العدد المتاح والإجمالي
- تحديث تلقائي عند الحجز

### 6. `shipments` (الشحنات)
- معلومات الشحن
- رقم التتبع
- شركة الشحن
- سجل حالات الشحنة
- العنوان

### 7. `rejection_details` (تفاصيل الرفض)
- سبب الرفض
- المستندات المطلوبة
- إمكانية إعادة التقديم

---

## 🔄 حالات الطلب (15 حالة)

### 1. `submitted` - تم الاستلام
**ما يحدث:**
- أول حالة بعد تقديم الطلب
- يتم تسجيل الطلب في النظام
- إرسال إشعار للمتقدم

**ما يظهر للمتقدم:**
- معلومات الطلب الأساسية
- رقم المعاملة
- Timeline يبدأ من هنا

---

### 2. `in_review` - قيد المراجعة
**ما يحدث:**
- الموظفون يراجعون المستندات
- التحقق من صحة البيانات

**ما يظهر للمتقدم:**
- مكون `ReviewStatus`
- عداد الأيام منذ التقديم
- معلومات عن عملية المراجعة
- شريط تقدم متحرك

**المكون المستخدم:** `src/components/ReviewStatus.jsx`

---

### 3. `approved` - تمت الموافقة
**ما يحدث:**
- تمت الموافقة على الطلب
- جاهز للانتقال للخطوة التالية

**ما يظهر للمتقدم:**
- رسالة الموافقة
- الخطوة التالية (دفع أو موعد)

---

### 4. `payment_pending` - في انتظار الدفع
**ما يحدث:**
- الطلب بحاجة للدفع
- يظهر نظام الدفع الكامل

**ما يظهر للمتقدم:**
- مكون `PaymentProcessor`
- 5 طرق دفع:
  - بطاقة ائتمان
  - مدى
  - Apple Pay
  - STC Pay
  - تحويل بنكي
- عرض المبلغ الإجمالي
- تفاصيل الرسوم

**المكون المستخدم:** `src/components/PaymentProcessor.jsx`

**الوظائف:**
- اختيار طريقة الدفع
- معالجة الدفع محلياً (Demo)
- تسجيل العملية في جدول `payments`
- تحديث الحالة تلقائياً إلى `payment_completed`
- إنشاء رقم عملية فريد

---

### 5. `payment_completed` - تم الدفع
**ما يحدث:**
- تم استلام الدفع بنجاح
- يتم عرض إيصال الدفع

**ما يظهر للمتقدم:**
- رقم العملية
- المبلغ المدفوع
- تاريخ الدفع
- طريقة الدفع

---

### 6. `appointment_required` - يتطلب حجز موعد
**ما يحدث:**
- الطلب بحاجة لزيارة القنصلية
- يظهر نظام حجز المواعيد

**ما يظهر للمتقدم:**
- مكون `AppointmentBooking`
- تقويم تفاعلي
- اختيار التاريخ
- اختيار الوقت
- المواعيد المتاحة فقط
- حجز فوري

**المكون المستخدم:** `src/components/AppointmentBooking.jsx`

**الوظائف:**
- عرض المواعيد المتاحة للـ 30 يوم القادمة
- تصفية حسب المنطقة
- حجز الموعد مباشرة
- تحديث تلقائي للأوقات المتاحة
- تأكيد الحجز برسالة
- إمكانية الإلغاء

---

### 7. `appointment_booked` - تم حجز الموعد
**ما يحدث:**
- تم حجز الموعد بنجاح

**ما يظهر للمتقدم:**
- تفاصيل الموعد (التاريخ، الوقت، المكان)
- ملاحظات مهمة
- إمكانية إلغاء الموعد

---

### 8. `processing` - قيد المعالجة
**ما يحدث:**
- الطلب قيد المعالجة الفعلية
- عدة مراحل داخلية

**ما يظهر للمتقدم:**
- مكون `ProcessingStatus`
- شريط تقدم ديناميكي (%)
- 4 مراحل معالجة:
  1. التحقق من المستندات
  2. عملية الموافقة
  3. إعداد الوثيقة
  4. المراجعة النهائية
- المرحلة الحالية مميزة
- التاريخ المتوقع للإنجاز
- رسوم متحركة

**المكون المستخدم:** `src/components/ProcessingStatus.jsx`

**الوظائف:**
- حساب نسبة الإنجاز تلقائياً
- تحديد المرحلة الحالية
- عرض التقدم بصرياً
- تقدير تاريخ الإنجاز

---

### 9. `ready` - جاهز للاستلام
**ما يحدث:**
- الطلب جاهز
- يمكن الاستلام أو الشحن

**ما يظهر للمتقدم:**
- رسالة الجاهزية
- خيارات الاستلام

---

### 10. `shipping` - جاري الشحن
**ما يحدث:**
- تم بدء عملية الشحن
- يظهر نظام التتبع الكامل

**ما يظهر للمتقدم:**
- مكون `ShippingTracker`
- رقم التتبع
- شركة الشحن
- الحالة الحالية
- مراحل الشحن:
  - قيد التجهيز
  - تم الشحن
  - في الطريق
  - تم التوصيل
- عنوان التوصيل
- التاريخ المتوقع

**المكون المستخدم:** `src/components/ShippingTracker.jsx`

**الوظائف:**
- عرض رقم التتبع
- timeline الشحنة
- تحديثات في الوقت الفعلي
- عرض العنوان

---

### 11. `shipped` - تم الشحن
**ما يحدث:**
- الشحنة في الطريق

**ما يظهر للمتقدم:**
- نفس مكون التتبع
- الحالة: في الطريق
- تحديثات الموقع

---

### 12. `delivered` - تم التوصيل
**ما يحدث:**
- وصلت الشحنة

**ما يظهر للمتقدم:**
- رسالة التوصيل الناجح
- تاريخ التوصيل الفعلي
- شكر للمتقدم

---

### 13. `completed` - مكتمل
**ما يحدث:**
- الطلب اكتمل بنجاح

**ما يظهر للمتقدم:**
- رسالة النجاح
- شكر للمتقدم
- إمكانية تقييم الخدمة

---

### 14. `rejected` - مرفوض
**ما يحدث:**
- تم رفض الطلب

**ما يظهر للمتقدم:**
- مكون `RejectionDetails`
- سبب الرفض واضح
- المستندات المطلوبة (إن وجدت)
- إمكانية إعادة التقديم (حسب الإعدادات)
- زر "إعادة تقديم الطلب"
- زر "تواصل معنا"

**المكون المستخدم:** `src/components/RejectionDetails.jsx`

**الوظائف:**
- عرض سبب الرفض
- قائمة المستندات المطلوبة
- زر إعادة التقديم (إذا مسموح)
- تاريخ الرفض

---

### 15. `cancelled` - ملغي
**ما يحدث:**
- تم إلغاء الطلب

**ما يظهر للمتقدم:**
- رسالة الإلغاء
- سبب الإلغاء
- معلومات الاتصال

---

## 🎨 المكونات المستخدمة

### للمتقدمين (User-Facing):

1. **`ReviewStatus.jsx`** - حالة المراجعة
2. **`PaymentProcessor.jsx`** - نظام الدفع الكامل
3. **`AppointmentBooking.jsx`** - حجز المواعيد
4. **`ProcessingStatus.jsx`** - حالة المعالجة
5. **`ShippingTracker.jsx`** - تتبع الشحنات
6. **`RejectionDetails.jsx`** - تفاصيل الرفض

### للإدارة (Admin-Facing):

7. **`AdminApplicationStatusManager.jsx`** - إدارة شاملة للحالة
8. **`AppointmentsShipmentsManagement.jsx`** - إدارة المواعيد والشحنات

---

## 📱 صفحة التتبع الرئيسية

**`src/pages/TransactionTracking.jsx`**

### الميزات:
- بحث برقم الطلب
- عرض تلقائي للمكون المناسب حسب الحالة
- Timeline ديناميكي
- بيانات المتقدم
- أزرار طباعة ونسخ
- تحديث تلقائي

### الآلية:
```javascript
const renderStatusSpecificContent = () => {
  switch (application?.status) {
    case 'in_review':
      return <ReviewStatus />;
    case 'payment_pending':
      return <PaymentProcessor />;
    case 'appointment_required':
      return <AppointmentBooking />;
    case 'processing':
      return <ProcessingStatus />;
    case 'shipping':
    case 'shipped':
    case 'delivered':
      return <ShippingTracker />;
    case 'rejected':
      return <RejectionDetails />;
    // ... الحالات الأخرى
  }
};
```

---

## 🔧 لوحة الإدارة

### 1. إدارة الحالة (`AdminApplicationStatusManager`)

**المسار:** يمكن إضافته في `ApplicationDetail.jsx`

**الوظائف:**
- اختيار الحالة الجديدة من قائمة شاملة
- إضافة ملاحظات للتغيير
- نموذج خاص للرفض:
  - إدخال سبب الرفض
  - تحديد المستندات المطلوبة
  - السماح/عدم السماح بإعادة التقديم
- تسجيل تلقائي في `status_history`
- تحديث فوري

### 2. إدارة المواعيد والشحنات (`AppointmentsShipmentsManagement`)

**المسار:** `/admin/appointments-shipments`

#### تبويب المواعيد:
- عرض جميع المواعيد
- بحث وتصفية
- تأكيد حضور المتقدم
- إلغاء المواعيد
- معلومات كاملة لكل موعد

#### تبويب الشحنات:
- عرض الطلبات الجاهزة للشحن
- إنشاء شحنة جديدة:
  - اختيار شركة الشحن
  - إدخال العنوان الكامل
  - تحديد التاريخ المتوقع
  - رقم تتبع تلقائي
- تحديث حالة الشحنة خطوة بخطوة:
  - قيد التجهيز → تم الشحن
  - تم الشحن → في الطريق
  - في الطريق → تم التوصيل
- بحث وتصفية

---

## 🔄 سير العمل الكامل (Workflow)

### مثال 1: طلب بسيط بدون دفع
```
submitted → in_review → approved → processing → ready → completed
```

### مثال 2: طلب يتطلب دفع وموعد
```
submitted → in_review → approved →
payment_pending → payment_completed →
appointment_required → appointment_booked →
processing → ready → completed
```

### مثال 3: طلب مع شحن
```
submitted → in_review → approved →
payment_pending → payment_completed →
processing → ready →
shipping → shipped → delivered
```

### مثال 4: طلب مرفوض
```
submitted → in_review → rejected
(مع إمكانية إعادة التقديم)
```

---

## 📊 الإحصائيات والتقارير

يمكن استخراج تقارير من:
- `status_history` - تاريخ كل التغييرات
- `payments` - تقارير الدفع
- `appointments` - إحصائيات الحجوزات
- `shipments` - تقارير الشحن

---

## 🔐 الأمان

1. **RLS Policies:** كل الجداول محمية
2. **Public Access:** يمكن للجميع القراءة للتتبع
3. **Staff Only:** فقط الموظفون يمكنهم التعديل
4. **Automatic Logging:** كل تغيير يُسجل تلقائياً

---

## 🚀 كيفية الاستخدام

### للمتقدمين:
1. اذهب إلى `/transaction/:referenceNumber`
2. أو `/transaction?ref=SUD-2025-1234`
3. أو ابحث في صفحة التتبع

### للإدارة:
1. اذهب إلى `/admin/applications`
2. اختر الطلب
3. استخدم `AdminApplicationStatusManager` لتحديث الحالة
4. أو اذهب إلى `/admin/appointments-shipments` لإدارة المواعيد والشحنات

---

## ✅ المزايا الرئيسية

1. **تغطية شاملة:** جميع الحالات الـ 15 مبرمجة بالكامل
2. **تفاعلية كاملة:** كل حالة لها واجهة مخصصة
3. **تحديثات فورية:** كل تغيير يُسجل ويُعرض فوراً
4. **سهولة الاستخدام:** واجهات بسيطة وواضحة
5. **شفافية كاملة:** المتقدم يرى كل شيء
6. **إدارة قوية:** لوحة تحكم شاملة للموظفين
7. **أمان عالي:** RLS + سجل كامل للتغييرات

---

## 🎯 النتيجة

**نظام تتبع متكامل 100%** يغطي كل حالة من البداية للنهاية!
