# دليل نظام الصلاحيات المتقدم للموظفين

## نظرة عامة

تم تطوير نظام صلاحيات متقدم يمنحك تحكماً كاملاً في:
1. ✅ الخدمات التي يمكن للموظف العمل عليها
2. ✅ المناطق التي يمكن للموظف التعامل معها
3. ✅ أقسام لوحة التحكم التي تظهر للموظف في القائمة الجانبية

---

## الميزات الجديدة

### 1️⃣ صلاحيات الخدمات

#### تحديد الخدمات بشكل فردي
- اختر خدمات محددة من القائمة
- كل خدمة تُعرض مع اسمها بالعربي
- يمكن تحديد أو إلغاء تحديد أي خدمة بنقرة واحدة

#### تحديد جميع الخدمات
- ✨ خيار "تحديد جميع الخدمات" في أعلى القسم
- عند تفعيله، يحصل الموظف على وصول كامل لجميع الخدمات
- مثالي لـ Managers والـ Supervisors
- عند تحديده، تختفي قائمة الخدمات الفردية
- يُظهر رسالة تأكيد "الموظف لديه وصول لجميع الخدمات"

**في قاعدة البيانات:**
- حقل `can_access_all_services` في جدول `staff`
- إذا كان `true`، لا حاجة للتحقق من `staff_services`

---

### 2️⃣ صلاحيات المناطق

#### تحديد المناطق بشكل فردي
- اختر مناطق محددة (الرياض، جدة، مكة، إلخ)
- كل منطقة تُعرض مع الاسم والرمز
- 16 منطقة متاحة في السعودية

#### تحديد جميع المناطق
- 🌍 خيار "تحديد جميع المناطق" في أعلى القسم
- عند تفعيله، يحصل الموظف على وصول كامل لجميع المناطق
- مثالي للموظفين الذين يعملون على مستوى المملكة
- عند تحديده، تختفي قائمة المناطق الفردية
- يُظهر رسالة تأكيد "الموظف لديه وصول لجميع المناطق"

**في قاعدة البيانات:**
- حقل `can_access_all_regions` في جدول `staff`
- جدول `staff_regions` لتخزين المناطق المحددة
- إذا كان `can_access_all_regions = true`، لا حاجة للتحقق من `staff_regions`

---

### 3️⃣ صلاحيات أقسام لوحة التحكم

#### الأقسام المتاحة:

1. **📊 لوحة التحكم الرئيسية** (`dashboard`)
   - الصفحة الرئيسية مع الإحصائيات

2. **📝 إدارة الطلبات** (`applications`)
   - عرض وإدارة طلبات الخدمات
   - معالجة واعتماد الطلبات

3. **⚙️ إدارة الخدمات** (`services`)
   - إضافة وتعديل الخدمات
   - إدارة الحقول والمتطلبات

4. **👥 إدارة الموظفين** (`staff`)
   - إضافة وتعديل الموظفين
   - إدارة الصلاحيات

5. **📰 إدارة المحتوى** (`content`)
   - إدارة الأخبار والفعاليات
   - تعديل محتوى الموقع

6. **💬 إدارة المحادثات** (`chat`)
   - الرد على استفسارات العملاء
   - إدارة المحادثات الحية

7. **📅 التقويم والمواعيد** (`calendar`)
   - عرض وإدارة المواعيد
   - جدولة المواعيد

8. **📈 التقارير** (`reports`)
   - عرض التقارير والإحصائيات
   - تحليل البيانات

#### كيف تعمل؟

- اختر الأقسام التي تريد أن يصل إليها الموظف
- **مهم:** الأقسام المحددة فقط هي التي ستظهر في القائمة الجانبية (Sidebar)
- إذا لم تحدد أي قسم، سيظهر تحذير باللون الأصفر
- يمكن تحديد أكثر من قسم
- كل قسم له أيقونة (emoji) واسم واضح

**في قاعدة البيانات:**
- حقل `permissions` (jsonb) في جدول `staff`
- يحتوي على `dashboard_sections` كـ array
- مثال: `{"dashboard_sections": ["dashboard", "applications", "chat"]}`

---

## تدفق البيانات

### عند إنشاء موظف جديد:

```javascript
// 1. إنشاء حساب المستخدم عبر Edge Function
POST /functions/v1/create-staff-user
{
  email, password, employee_number, ...
}

// 2. تحديث الصلاحيات في جدول staff
UPDATE staff SET
  permissions = {
    "dashboard_sections": ["dashboard", "applications"],
    "allowed_regions": [region_ids]
  },
  can_access_all_services = false,
  can_access_all_regions = false
WHERE id = staff_id

// 3. ربط الخدمات المحددة (إذا لم يتم تحديد الكل)
INSERT INTO staff_services (staff_id, service_id, ...)
VALUES ...

// 4. ربط المناطق المحددة (إذا لم يتم تحديد الكل)
INSERT INTO staff_regions (staff_id, region_id)
VALUES ...
```

---

## أمثلة الاستخدام

### مثال 1: موظف خدمة عملاء
- **الخدمات:** محددة (الاستعلامات العامة، الشكاوى)
- **المناطق:** الرياض وجدة فقط
- **أقسام لوحة التحكم:**
  - 📊 لوحة التحكم الرئيسية
  - 📝 إدارة الطلبات
  - 💬 إدارة المحادثات

### مثال 2: مشرف عام
- **الخدمات:** ✨ جميع الخدمات
- **المناطق:** 🌍 جميع المناطق
- **أقسام لوحة التحكم:**
  - 📊 لوحة التحكم الرئيسية
  - 📝 إدارة الطلبات
  - 👥 إدارة الموظفين
  - 📅 التقويم والمواعيد
  - 📈 التقارير

### مثال 3: محرر محتوى
- **الخدمات:** لا يحتاج (لا يعمل على الخدمات)
- **المناطق:** جميع المناطق
- **أقسام لوحة التحكم:**
  - 📊 لوحة التحكم الرئيسية
  - 📰 إدارة المحتوى

### مثال 4: موظف جوازات - منطقة مكة
- **الخدمات:** الجوازات فقط (تجديد، استخراج، إضافة مولود)
- **المناطق:** مكة المكرمة فقط
- **أقسام لوحة التحكم:**
  - 📊 لوحة التحكم الرئيسية
  - 📝 إدارة الطلبات
  - 📅 التقويم والمواعيد

---

## التحقق من الصلاحيات (للمطورين)

### في Frontend (React):

```javascript
// قراءة بيانات الموظف
const { data: staffData } = await supabase
  .from('staff')
  .select(`
    *,
    permissions,
    can_access_all_services,
    can_access_all_regions
  `)
  .eq('user_id', user.id)
  .single();

// التحقق من صلاحية الوصول لقسم معين
const canAccessSection = (sectionId) => {
  const sections = staffData?.permissions?.dashboard_sections || [];
  return sections.includes(sectionId);
};

// التحقق من صلاحية الوصول لخدمة معينة
const canAccessService = async (serviceId) => {
  // إذا كان له وصول لجميع الخدمات
  if (staffData.can_access_all_services) {
    return true;
  }

  // تحقق من staff_services
  const { data } = await supabase
    .from('staff_services')
    .select('*')
    .eq('staff_id', staffData.id)
    .eq('service_id', serviceId)
    .maybeSingle();

  return !!data;
};

// التحقق من صلاحية الوصول لمنطقة معينة
const canAccessRegion = async (regionId) => {
  // إذا كان له وصول لجميع المناطق
  if (staffData.can_access_all_regions) {
    return true;
  }

  // تحقق من staff_regions
  const { data } = await supabase
    .from('staff_regions')
    .select('*')
    .eq('staff_id', staffData.id)
    .eq('region_id', regionId)
    .maybeSingle();

  return !!data;
};
```

### في AdminSidebar (عرض الأقسام المسموحة فقط):

```javascript
const AdminSidebar = () => {
  const [allowedSections, setAllowedSections] = useState([]);

  useEffect(() => {
    const loadPermissions = async () => {
      const { data: { user } } = await supabase.auth.getUser();

      const { data: staffData } = await supabase
        .from('staff')
        .select('permissions')
        .eq('user_id', user.id)
        .single();

      setAllowedSections(
        staffData?.permissions?.dashboard_sections || []
      );
    };

    loadPermissions();
  }, []);

  // عرض الروابط المسموحة فقط
  const menuItems = [
    { id: 'dashboard', icon: '📊', label: 'لوحة التحكم', path: '/admin' },
    { id: 'applications', icon: '📝', label: 'الطلبات', path: '/admin/applications' },
    { id: 'services', icon: '⚙️', label: 'الخدمات', path: '/admin/services' },
    { id: 'staff', icon: '👥', label: 'الموظفين', path: '/admin/staff' },
    // ... إلخ
  ];

  return (
    <div className="sidebar">
      {menuItems.map(item => {
        // عرض الرابط فقط إذا كان موجوداً في الصلاحيات
        if (!allowedSections.includes(item.id)) {
          return null;
        }

        return (
          <Link key={item.id} to={item.path}>
            <span>{item.icon}</span>
            <span>{item.label}</span>
          </Link>
        );
      })}
    </div>
  );
};
```

---

## قاعدة البيانات

### جدول `staff` - الحقول الجديدة:

```sql
-- صلاحيات الموظف (أقسام لوحة التحكم والمناطق)
permissions jsonb DEFAULT '{
  "dashboard_sections": [],
  "allowed_regions": []
}'::jsonb

-- وصول كامل لجميع الخدمات؟
can_access_all_services boolean DEFAULT false

-- وصول كامل لجميع المناطق؟
can_access_all_regions boolean DEFAULT false
```

### جدول `staff_regions` (جديد):

```sql
CREATE TABLE staff_regions (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  staff_id uuid REFERENCES staff(id) ON DELETE CASCADE,
  region_id uuid REFERENCES regions(id) ON DELETE CASCADE,
  created_at timestamptz DEFAULT now(),
  UNIQUE(staff_id, region_id)
);
```

### Indexes للأداء:

```sql
-- للبحث في JSONB
CREATE INDEX idx_staff_permissions ON staff USING gin (permissions);

-- للبحث السريع في الموظفين بصلاحيات كاملة
CREATE INDEX idx_staff_all_services
  ON staff (can_access_all_services)
  WHERE can_access_all_services = true;

CREATE INDEX idx_staff_all_regions
  ON staff (can_access_all_regions)
  WHERE can_access_all_regions = true;

-- للبحث في المناطق
CREATE INDEX idx_staff_regions_staff_id ON staff_regions(staff_id);
CREATE INDEX idx_staff_regions_region_id ON staff_regions(region_id);
```

---

## سيناريوهات الاستخدام

### السيناريو 1: موظف يسجل دخول

1. تسجيل الدخول بالبريد وكلمة المرور
2. النظام يقرأ `permissions` من `staff`
3. يُعرض في Sidebar الأقسام الموجودة في `dashboard_sections` فقط
4. عند فتح قسم "الطلبات"، يعرض فقط طلبات:
   - الخدمات المسموح له بها
   - المناطق المسموح له بها

### السيناريو 2: إضافة موظف جديد

1. Admin يملأ نموذج إضافة موظف
2. يختار الخدمات (أو جميع الخدمات)
3. يختار المناطق (أو جميع المناطق)
4. يختار أقسام لوحة التحكم
5. عند الحفظ:
   - ينشئ حساب في auth.users
   - ينشئ سجل في staff مع الصلاحيات
   - يربط الخدمات في staff_services (إن وُجدت)
   - يربط المناطق في staff_regions (إن وُجدت)

### السيناريو 3: تعديل صلاحيات موظف

```javascript
// تحديث أقسام لوحة التحكم
await supabase
  .from('staff')
  .update({
    permissions: {
      dashboard_sections: ['dashboard', 'applications', 'chat'],
      allowed_regions: existingRegions
    }
  })
  .eq('id', staffId);

// إضافة خدمة جديدة
await supabase
  .from('staff_services')
  .insert({
    staff_id: staffId,
    service_id: newServiceId,
    can_process: true,
    can_approve: false,
    can_view: true
  });

// إضافة منطقة جديدة
await supabase
  .from('staff_regions')
  .insert({
    staff_id: staffId,
    region_id: newRegionId
  });
```

---

## نصائح وأفضل الممارسات

### للمديرين (Admins):

1. **استخدم "جميع الخدمات"** للمشرفين العامين فقط
2. **استخدم "جميع المناطق"** للموظفين الذين يعملون على مستوى المملكة
3. **حدد الأقسام بعناية** - لا تعطِ صلاحيات غير ضرورية
4. **راجع الصلاحيات دورياً** لضمان الأمان

### للمطورين:

1. **تحقق دائماً من الصلاحيات** قبل عرض البيانات
2. **استخدم RLS Policies** لحماية البيانات من جهة الخادم
3. **احفظ الصلاحيات في cache** لتحسين الأداء
4. **وثّق التغييرات** في الصلاحيات (audit log)

---

## استكشاف الأخطاء

### "الموظف لا يرى أي أقسام في Sidebar"
- **الحل:** تأكد من أن `dashboard_sections` ليس فارغاً
- تحقق من قيمة `permissions` في قاعدة البيانات

### "الموظف يرى خدمات ليست مخصصة له"
- **الحل:** تحقق من `can_access_all_services`
- راجع `staff_services` للتأكد من الربط الصحيح

### "الموظف لا يمكنه الوصول لمنطقة معينة"
- **الحل:** تحقق من `can_access_all_regions`
- راجع `staff_regions` للتأكد من وجود الربط

---

## ملخص

### ✅ ما تم إضافته:

1. **نظام صلاحيات الخدمات:**
   - تحديد فردي أو جميع الخدمات
   - حقل `can_access_all_services` في قاعدة البيانات

2. **نظام صلاحيات المناطق:**
   - تحديد فردي أو جميع المناطق
   - جدول `staff_regions` جديد
   - حقل `can_access_all_regions` في قاعدة البيانات

3. **نظام صلاحيات أقسام لوحة التحكم:**
   - 8 أقسام متاحة
   - تُخزن في `permissions.dashboard_sections`
   - تحدد ما يظهر في Sidebar

### 🎯 الفوائد:

- **أمان محسّن:** كل موظف يرى فقط ما يحتاجه
- **مرونة عالية:** تحكم دقيق في الصلاحيات
- **سهولة الإدارة:** واجهة واضحة لتعيين الصلاحيات
- **أداء أفضل:** استخدام indexes وتحسينات SQL

**النظام الآن جاهز للاستخدام! 🎉**
